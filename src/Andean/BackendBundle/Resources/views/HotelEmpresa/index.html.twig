{% extends '::backend.html.twig' %}

{% block id 'hotelempresa' %}
{% block title %}Listado de empresas de hotel{% endblock %}

{% block content %}
<table>
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Categoría</th>
            <th>Ciudad</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <th>{{ entity.nombre }}</th>
            <th>{{ entity.categoria }}</th>
            <th>{{ entity.ciudad }}</th>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('backend_hotelempresa_edit', { 'id': entity.id }) }}">Modificar</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
{% block button_area %}
<a class="button_primary button continue" href="{{ path('backend_hotelempresa_new') }}">
  Añadir una nueva empresa
</a>
{% endblock %}