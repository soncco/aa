{% extends '::backend.html.twig' %}

{% block id 'trentipo' %}
{% block title %}Modificar el horario {{ entity }}{% endblock %}

{% block content %}
<form action="{{ path('backend_trentipo_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    <div class="input_wrapper">
    {{ form_row(edit_form.numero) }}
    {{ form_row(edit_form.empresa) }}
    {{ form_row(edit_form.nombre) }}
    </div>
    {#{ form_widget(edit_form) }#}
    <p>
        <button class="button_primary button continue" type="submit">Guardar cambios</button>
    </p>
    {{ form_rest(edit_form) }}
</form>
{% endblock %}
{% block button_area %}
<a href="{{ path('backend_trentipo') }}" class="button"><div class="icon back"></div>Volver al listado</a>
{% endblock %}